apiVersion: v1
kind: Service
metadata:
  name: spring-boot-demo
  labels:
    name: spring-boot-demo
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: spring-boot-demo
  type: LoadBalancer